 platforms:
   type: dictionary
   displayName: Platforms
   besql: true
   primaryKey: ID
   scheme:
     columns:
     - ID:
         type: KEYTYPE
         primaryKey: true
     - title:
         type: VARCHAR(100)
     - GPL:
         doc: Reference to GEO platforms
         type: VARCHAR(10)
   queries: []
